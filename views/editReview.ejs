<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Store Review App</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Store Review App</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/addStore">Add new review</a>
              </li> 
              <li class="nav-item">
                <a class="nav-link" href="/reviews">View all Reviews</a>
              </li> 

            </ul>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Edit Review</h2></div>
        <br>
        <div class="text-center">
          <% if (review) { %> 
            <form action="/editReview/<%= review.reviewID %>" method="POST">
                <label for="name">Store Name:</label><br>
                <input type="text" id="name" name="name" value="<%= review.storeName %>" disabled><br><br>
                
                <label for="title">Review Subject:</label><br>
                <input type="text" id="title" name="title" value="<%= review.title %>" required><br><br>

                <label for="rating">Rating:</label><br>
                <input type="number" id="rating" name="rating" min="0" max="10" value="<%= review.rating %>" required><br><br>

                <label for="review">Review:</label><br>
                <textarea id="review" name="review" rows="4" cols="50" value="<%= review.review %>"></textarea>
                <br><br>
                <button type="submit">Update Review</button>
            </form>
          <% } else { %>
              <p>No review found.</p>
          <% } %>
          <a href ="/">Back</a>
        </div>
    </div>
</body>
</html>
<!--See Yuheng 23023660-->