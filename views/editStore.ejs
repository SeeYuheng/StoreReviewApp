<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
        <title>Store Review App</title>
    </head>
<body>
    <nav class="navbar navbar-expand-sm bg-dark navbar-dark">
        <div class="container-fluid">
          <a class="navbar-brand" href="/">Store Review App</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavbar">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="collapsibleNavbar">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link" href="/addStore">Add new store</a>
              </li> 
              <li class="nav-item">
                <a class="nav-link" href="/reviews">View all Reviews</a>
              </li> 
 
            </ul>
          </div>
        </div>
    </nav>

    <div class="container">
        <br>
        <div class="text-center"><h2>Update Store</h2></div>
        <br>
        <div class="text-center">
          <% if (store) { %> 
            <form action="/editStore/<%= store.storeID %>" method="POST" enctype="multipart/form-data">
                <label for="name">Store Name:</label><br>
                <input type="text" id="name" name="name" value = "<%= store.storeName %>" required><br><br>

                <label for="location">Location:</label><br>
                <input type="text" id="location" name="location" value = "<%= store.location %>" required><br><br>

                <label for="description">Description:</label><br>
                <input type="text" id="description" name="description" value = "<%= store.description %>" required><br><br>

                <label for="image">Current Image:</label><br>
                <input type="text" name="currentImage" value="<%= store.image %>" disabled><br>
                <img src = "/images/<%= store.image %>" width="20%"><br><br>
                New Image:<br> <input type="file" id="image" name="image" accept="image/*" value= <%= store.image %> ><br><br>
                <button type="submit">Update Store</button>
            </form>
          <% } else { %>
              <p>No store found.</p>
          <% } %>
          <a href ="/">Back</a>
        </div>
    </div>
</body>
</html>
<!--See Yuheng 23023660-->